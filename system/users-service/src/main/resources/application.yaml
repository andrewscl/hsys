server:
  port: 8083
  forward-headers-strategy: framework

spring:
  application:
    name: users-service
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

  datasource:
    url: jdbc:postgresql://db-system:5432/hsys_users
    username: ${POSTGRES_HSYSDB_USERNAME}
    password: ${POSTGRES_HSYSDB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  health:
    probes:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
